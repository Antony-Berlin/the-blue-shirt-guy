# ── Core runtime ──────────────────────────────────────────────────────────────
openenv-core>=0.2.0
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# ── Environment server ────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.23.0
fastmcp>=3.0.0

# ── Agent tools ───────────────────────────────────────────────────────────────
duckduckgo-search>=6.0.0
pyflakes>=3.0.0

# ── Notebook (self_improve.ipynb) ─────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.0.0
matplotlib>=3.7.0

# ── GRPO fine-tuning (training/grpo_trainer.py) ───────────────────────────────
# Requires a CUDA GPU.  Skip this section if you only need inference/eval.
transformers>=4.40.0
accelerate>=0.30.0
peft>=0.10.0
trl>=0.8.0
# bitsandbytes is Linux/CUDA only — comment out on Mac or CPU-only machines
# bitsandbytes>=0.43.0
